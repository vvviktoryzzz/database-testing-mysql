# Test Cases — Database Testing

## TC-DB-001 — Проверка наличия данных в таблице customers

**Предусловия:**

* База данных создана.
* Тестовые данные добавлены.

**Шаги:**

1. Выполнить `SELECT * FROM customers`.

**Ожидаемый результат:**

* Запрос выполняется успешно.
* Возвращаются записи клиентов.
* Каждая запись содержит `customer_id`, `first_name`, `last_name`, `email`, `city`.

**Фактический результат:**

* Запрос выполнен успешно.
* Возвращено 4 записи со всеми ожидаемыми полями.

**Статус:** PASS

---

## TC-DB-002 — Проверка количества записей в основных таблицах

**Предусловия:**

* Тестовые данные добавлены во все таблицы.

**Шаги:**

1. Выполнить запрос для подсчёта количества записей в `customers`, `products`, `orders` и `order_items`.

**Ожидаемый результат:**

* `customers` содержит 4 записи.
* `products` содержит 5 записей.
* `orders` содержит 4 записи.
* `order_items` содержит 6 записей.

**Фактический результат:**

* `customers` — 4 записи.
* `products` — 5 записей.
* `orders` — 4 записи.
* `order_items` — 6 записей.

**Статус:** PASS

---

## TC-DB-003 — Проверка фильтрации клиентов по городу

**Предусловия:**

* В таблице `customers` присутствуют тестовые данные.

**Шаги:**

1. Выполнить запрос с условием `city = 'Berlin'`.

**Ожидаемый результат:**

* Возвращаются только клиенты из Berlin.

**Фактический результат:**

* Найдены 2 клиента из Berlin: Anna Schmidt и Laura Fischer.

**Статус:** PASS

---

## TC-DB-004 — Проверка уникальности email

**Предусловия:**

* В таблице `customers` присутствуют тестовые данные.

**Шаги:**

1. Сгруппировать клиентов по `email`.
2. Использовать `HAVING COUNT(*) > 1`.

**Ожидаемый результат:**

* Дубликаты email отсутствуют.

**Фактический результат:**

* Дубликаты email не обнаружены.

**Статус:** PASS

---

## TC-DB-005 — Проверка товаров с некорректной ценой

**Предусловия:**

* В таблице `products` присутствуют тестовые данные.

**Шаги:**

1. Найти товары, у которых `price IS NULL OR price <= 0`.

**Ожидаемый результат:**

* Товары с отсутствующей, нулевой или отрицательной ценой отсутствуют.

**Фактический результат:**

* Запрос вернул `Empty set`.
* Некорректных значений цены не обнаружено.

**Статус:** PASS

---

## TC-DB-006 — Проверка товаров без категории

**Предусловия:**

* В таблице `products` присутствуют тестовые данные.

**Шаги:**

1. Найти записи, где `category IS NULL`.

**Ожидаемый результат:**

* Товары без категории отсутствуют.

**Фактический результат:**

* Запрос вернул `Empty set`.

**Статус:** PASS

---

## TC-DB-007 — Проверка заказов без клиента

**Предусловия:**

* В таблице `orders` присутствуют тестовые данные.

**Шаги:**

1. Найти заказы, где `customer_id IS NULL`.

**Ожидаемый результат:**

* Заказы без клиента отсутствуют.

**Фактический результат:**

* Запрос вернул `Empty set`.

**Статус:** PASS

---

## TC-DB-008 — Проверка связи orders и customers

**Предусловия:**

* В таблицах `orders` и `customers` присутствуют данные.

**Шаги:**

1. Выполнить `JOIN` таблиц `orders` и `customers` по `customer_id`.

**Ожидаемый результат:**

* Каждый заказ связан с существующим клиентом.

**Фактический результат:**

* Все 4 заказа успешно связаны с существующими клиентами.

**Статус:** PASS

---

## TC-DB-009 — Проверка связи order_items и products

**Предусловия:**

* В таблицах `order_items` и `products` присутствуют данные.

**Шаги:**

1. Выполнить `JOIN` таблиц `order_items` и `products` по `product_id`.

**Ожидаемый результат:**

* Каждая позиция заказа связана с существующим товаром.

**Фактический результат:**

* Все 6 позиций заказов успешно связаны с существующими товарами.

**Статус:** PASS

---

## TC-DB-010 — Проверка количества заказов каждого клиента

**Предусловия:**

* В таблице `orders` присутствуют данные.

**Шаги:**

1. Сгруппировать заказы по `customer_id`.
2. Использовать `COUNT(order_id)`.

**Ожидаемый результат:**

* Для каждого клиента отображается корректное количество заказов.

**Фактический результат:**

* Клиент 1 — 2 заказа.
* Клиент 2 — 1 заказ.
* Клиент 3 — 1 заказ.

**Статус:** PASS

---

## TC-DB-011 — Проверка общего количества заказанных товаров

**Предусловия:**

* В таблице `order_items` присутствуют данные.

**Шаги:**

1. Сгруппировать записи по `product_id`.
2. Использовать `SUM(quantity)`.

**Ожидаемый результат:**

* Для каждого товара отображается корректное общее количество заказанных единиц.

**Фактический результат:**

* Product 1 — 1 единица.
* Product 2 — 1 единица.
* Product 3 — 5 единиц.
* Product 4 — 1 единица.
* Product 5 — 1 единица.

**Статус:** PASS

---

## TC-DB-012 — Проверка количества товаров в заказах

**Предусловия:**

* В таблице `order_items` присутствуют данные.

**Шаги:**

1. Сгруппировать позиции по `order_id`.
2. Использовать `SUM(quantity)`.

**Ожидаемый результат:**

* Для каждого заказа отображается корректное количество товаров.

**Фактический результат:**

* Заказ 1 — 3 единицы.
* Заказ 2 — 2 единицы.
* Заказ 3 — 1 единица.
* Заказ 4 — 3 единицы.

**Статус:** PASS

---

## TC-DB-013 — Проверка корректности количества товаров

**Предусловия:**

* В таблице `order_items` присутствуют тестовые данные.

**Шаги:**

1. Найти записи, где `quantity IS NULL OR quantity <= 0`.

**Ожидаемый результат:**

* Количество товара всегда заполнено и больше 0.

**Фактический результат:**

* Запрос вернул `Empty set`.
* Некорректных значений `quantity` не обнаружено.

**Статус:** PASS

---

## TC-DB-014 — Проверка средней цены товаров

**Предусловия:**

* В таблице `products` присутствуют тестовые данные.

**Шаги:**

1. Выполнить `AVG(price)`.

**Ожидаемый результат:**

* Средняя цена рассчитывается корректно.

**Фактический результат:**

* Средняя цена товаров — `64.99`.

**Статус:** PASS

---

## TC-DB-015 — Проверка статусов заказов

**Предусловия:**

* В таблице `orders` присутствуют тестовые данные.

**Шаги:**

1. Выполнить `SELECT DISTINCT status FROM orders`.

**Ожидаемый результат:**

* В данных используются только допустимые статусы: `Completed`, `Processing`, `Cancelled`.

**Фактический результат:**

* Обнаружены статусы `Completed`, `Processing`, `Cancelled`.
* Других значений нет.

**Статус:** PASS

---

## TC-DB-016 — Проверка Primary Key в таблице customers

**Предусловия:**

* Таблица `customers` создана.

**Шаги:**

1. Выполнить `SHOW CREATE TABLE customers`.

**Ожидаемый результат:**

* Поле `customer_id` является Primary Key.

**Фактический результат:**

* `customer_id` задан как Primary Key.

**Статус:** PASS

---

## TC-DB-017 — Проверка Primary Key и Foreign Key в таблице orders

**Предусловия:**

* Таблица `orders` создана.

**Шаги:**

1. Выполнить `SHOW CREATE TABLE orders`.

**Ожидаемый результат:**

* `order_id` является Primary Key.
* `customer_id` является Foreign Key и ссылается на `customers.customer_id`.

**Фактический результат:**

* `order_id` является Primary Key.
* `customer_id` ссылается на `customers.customer_id`.

**Статус:** PASS

---

## TC-DB-018 — Проверка Primary Key и Foreign Key в таблице order_items

**Предусловия:**

* Таблица `order_items` создана.

**Шаги:**

1. Выполнить `SHOW CREATE TABLE order_items`.

**Ожидаемый результат:**

* `order_item_id` является Primary Key.
* `order_id` является Foreign Key к `orders.order_id`.
* `product_id` является Foreign Key к `products.product_id`.

**Фактический результат:**

* Все ожидаемые ключи присутствуют.

**Статус:** PASS

---

## TC-DB-019 — Проверка Primary Key и обязательных полей в products

**Предусловия:**

* Таблица `products` создана.

**Шаги:**

1. Выполнить `SHOW CREATE TABLE products`.

**Ожидаемый результат:**

* `product_id` является Primary Key.
* `product_name` и `price` имеют ограничение `NOT NULL`.
* Поле `price` имеет тип `DECIMAL(10,2)`.

**Фактический результат:**

* Все ожидаемые ограничения присутствуют.

**Статус:** PASS

---

## TC-DB-020 — Проверка UNION

**Предусловия:**

* В таблицах `customers` и `products` присутствуют данные.

**Шаги:**

1. Объединить имена клиентов и названия товаров с помощью `UNION`.

**Ожидаемый результат:**

* Результаты обоих запросов объединяются в один набор данных без дубликатов.

**Фактический результат:**

* Получено 9 уникальных значений.

**Статус:** PASS

---

## Test Summary

**Total Test Cases:** 20

**PASS:** 20

**FAIL:** 0

**Ожидаемый результат:**

* Должны быть найдены товары с отсутствующей, нулевой или отрицательной ценой, если такие записи существуют.
* В корректных тестовых данных такие записи отсутствуют.

**Фактический результат:**

* Товаров с `NULL`, нулевой или отрицательной ценой не найдено.

**Статус:** PASS

---

## TC-DB-004 — Проверка уникальности email клиентов

**Предусловия:**

* В таблице `customers` присутствуют тестовые данные.

**Шаги:**

1. Сгруппировать клиентов по полю `email`.
2. Использовать `HAVING COUNT(*) > 1` для поиска дубликатов.

**Ожидаемый результат:**

* Дубликаты email отсутствуют.

**Фактический результат:**

* Дубликаты email не обнаружены.

**Статус:** PASS

---

## TC-DB-005 — Проверка связи заказов с клиентами

**Предусловия:**

* В таблицах `customers` и `orders` присутствуют данные.

**Шаги:**

1. Выполнить `JOIN` таблиц `orders` и `customers` по полю `customer_id`.

**Ожидаемый результат:**

* Каждый заказ связан с существующим клиентом.
* Для каждого заказа отображается информация о соответствующем клиенте.

**Фактический результат:**

* Все заказы успешно связаны с существующими клиентами.

**Статус:** PASS

---

## TC-DB-006 — Проверка связи позиций заказа с товарами

**Предусловия:**

* В таблицах `order_items` и `products` присутствуют данные.

**Шаги:**

1. Выполнить `JOIN` таблиц `order_items` и `products` по полю `product_id`.

**Ожидаемый результат:**

* Каждая позиция заказа связана с существующим товаром.
* Для каждой позиции отображается информация о соответствующем товаре.

**Фактический результат:**

* Все позиции заказов успешно связаны с существующими товарами.

**Статус:** PASS

---

## TC-DB-007 — Проверка количества заказов каждого клиента

**Предусловия:**

* В таблице `orders` присутствуют данные.

**Шаги:**

1. Сгруппировать заказы по `customer_id`.
2. Использовать `COUNT(order_id)` для подсчёта количества заказов.

**Ожидаемый результат:**

* Для каждого клиента отображается корректное количество заказов.

**Фактический результат:**

* Количество заказов для каждого клиента рассчитано корректно.

**Статус:** PASS

---

## TC-DB-008 — Проверка общего количества заказанных товаров

**Предусловия:**

* В таблице `order_items` присутствуют данные.

**Шаги:**

1. Сгруппировать записи по `product_id`.
2. Использовать `SUM(quantity)` для расчёта общего количества.

**Ожидаемый результат:**

* Для каждого товара отображается общее количество единиц, указанное в заказах.

**Фактический результат:**

* Общее количество единиц для каждого товара рассчитано корректно.

**Статус:** PASS

---

## TC-DB-009 — Проверка товаров без категории

**Предусловия:**

* В таблице `products` присутствуют тестовые данные.

**Шаги:**

1. Выполнить запрос для поиска записей, где `category IS NULL`.

**Ожидаемый результат:**

* Должны быть найдены товары без категории, если такие записи существуют.

**Фактический результат:**

* Товары без категории не обнаружены.

**Статус:** PASS

---

## TC-DB-010 — Проверка количества товаров в каждом заказе

**Предусловия:**

* В таблице `order_items` присутствуют данные.

**Шаги:**

1. Сгруппировать позиции по `order_id`.
2. Использовать `SUM(quantity)` для расчёта общего количества товаров.

**Ожидаемый результат:**

* Для каждого заказа отображается корректное общее количество товаров.

**Фактический результат:**

* Общее количество товаров для каждого заказа рассчитано корректно.

**Статус:** PASS

---

## Результаты тестирования

**Всего тест-кейсов:** 10

**PASS:** 10

**FAIL:** 0
